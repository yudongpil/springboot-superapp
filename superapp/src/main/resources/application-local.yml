server:
  port:8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/superapp_service_db
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true